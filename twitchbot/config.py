#!/usr/bin/env python

i_channel_list = ['teutatas']
admin_n_list = ['teutatas']

# examples

# i_channel_list = ['teutatas', 'other channel names...']       # everything needs to be lowercase
# admin_n_list = ['teutatas', 'other channel names...']         # everything needs to be lowercase